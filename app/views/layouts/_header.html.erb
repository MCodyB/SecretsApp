<header>
	<!-- Wrapper keeps height of page constant so scrollTop doesn't go all wonky from resizing -->
	<div class="wrapper">
			<ul class="utility links fixed">
			<% if signed_in?  %>
				<li><%= link_to current_user.username, user_url(current_user) %></li>
				<li><%= link_to "LogOut", logout_url %></li>
			<% else %>
				<li><%= link_to "New User", new_user_url %></li>
				<li><%= link_to "LogIn", new_session_url %></li>
			<% end %>
			</ul>
			<p><%= link_to "SecretsApp", root_url %></p>
	</div>
	
	<div class='navbar'>
			<ul class="nav links">
				<li><%= link_to "Secrets", secrets_url %></li>

				<li><span>Friends</span><%#= link_to "Friends", users_url %>
					<ul><li>COMING SOON!</li></ul>
				</li>
				
				<!-- <li>Users
					<ul class="links drop-menu">
						<%# User.all.each do |song| %>
							<%# if user.id % 3 == 0 %>
							<li><%#= link_to user.username, user_url(song.id) %></li>
							<%# end %>
						<%# end %>
					</ul>
				</li> -->
				<li>
					<%= link_to "Users", users_url %>
					<ul class="links drop-menu">
						<% User.all.each do |user| %>
							<li class="user-level"><%= link_to user.username, user_url(user.id) %></li>
						<% end %>
					</ul>
				</li>
			</ul>
	</div>
</header>